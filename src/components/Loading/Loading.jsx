const Loading = () => {
  return (
  <div className=" flex justify-center items-center">
    Data is Loading ....<span className="loading loading-ring loading-lg"></span>
  </div>);
};

export default Loading;
